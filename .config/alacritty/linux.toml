# Alacritty configuration, connecting to single tmux session 'base'.

[shell]
args = ["new-session", "-A", "-s", "base"]
program = "tmux"

# Window setup.

[window]
decorations = "None"
startup_mode = "Maximized"
